#include <stdio.h>
#include <stdlib.h>

typedef struct dados_unidade
{
   char nomedaunidade[50];
   char localizacao[20];
   char horariodefuncionamento[20];
   char telefonedecontato[20];

}unidade;
